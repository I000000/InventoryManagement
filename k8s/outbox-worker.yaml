apiVersion: apps/v1
kind: Deployment
metadata:
  name: outbox-worker
  namespace: inventory-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outbox-worker
  template:
    metadata:
      labels:
        app: outbox-worker
    spec:
      containers:
      - name: outbox
        image: sadsadsadsad/outbox-worker:latest
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: inventory-config
        - secretRef:
            name: inventory-secrets
        env:
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: inventory-secrets
              key: DB_PASS
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: outbox-worker
  namespace: inventory-system
spec:
  selector:
    app: outbox-worker
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP