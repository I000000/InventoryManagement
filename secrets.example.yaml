apiVersion: v1
kind: Secret
metadata:
  name: inventory-secrets
  namespace: inventory-system
type: Opaque
data:
  # Замените на свои значения в base64:
  # echo -n 'inventory' | base64   →   aW52ZW50b3J5
  DB_PASS: <base64-encoded-password>
  CLICKHOUSE_PASS: <base64-encoded-password>